//function somar () {
    //let v1 = 10;
    //let v2 = 10;
    //const resultado = v1 + v2 
    //console.log(resultado);
//}

//const soma = somar()

function somar (v1, v2) {
    return v1 + v2;
}


// funçao que retorna porcentagem de um valor 

function calcPorcentagem ( visualViewport, taxa) {
    return valor * (taxa / 100);

}


// função a prazo sem juros

let calcPagPrazo = (valor, taxa) => {
    return valor + (valor * (taxa / 100 ));
}


let calcPagVista = (valor, taxa) => {
    return valor + (valor * (taxa / 100 ));
}



